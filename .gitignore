# Datasets — too large for GitHub (CICIDS2017/UNSW-NB15 CSVs run into the
# hundreds of MB, well past GitHub's 100MB hard file-size limit). Download
# them yourself per data/README.md; only the README under data/ is tracked.
data/raw/
data/**/*.csv

# Runtime outputs — regenerated by the pipeline, not source.
results/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
venv/
.venv/
env/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Streamlit
.streamlit/
